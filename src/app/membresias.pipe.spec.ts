import { MembresiasPipe } from './membresias.pipe';

describe('MembresiasPipe', () => {
  it('create an instance', () => {
    const pipe = new MembresiasPipe();
    expect(pipe).toBeTruthy();
  });
});
